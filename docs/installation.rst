============
Installation
============

At the command line::

    $ easy_install django-telnyx

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-telnyx
    $ pip install django-telnyx
